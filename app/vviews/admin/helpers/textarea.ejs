<%
    textareaHelper = (name, content) => {
        return `<textarea class="form-control form-control-sm summernote" id="summernote" name="${name}" rows="2">
                    ${content}
                </textarea>`;
    }
%>