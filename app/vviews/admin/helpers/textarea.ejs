<%
    textareaHelper = (name, content) => {
        return `<textarea class="form-control form-control-sm" name="${name}" rows="2" id="ck_editor">
                    ${content}
                </textarea>`;
    }
%>
